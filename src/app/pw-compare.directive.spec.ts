import { PwCompareDirective } from './pw-compare.directive';

describe('PwCompareDirective', () => {
  it('should create an instance', () => {
    const directive = new PwCompareDirective();
    expect(directive).toBeTruthy();
  });
});
